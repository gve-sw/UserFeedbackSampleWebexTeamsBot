# vars file. The adapter section **needs** to be here
adapter:
    name: WebexTeamsWebhookManager
    authentication:
      access_token: <add access token here>
    parameters:
  
  # Add your variables from here on
remote_prefix: "<add remote prefix here>"